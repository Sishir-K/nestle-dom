# Core data handling and optimization
pandas==3.0.3
numpy==2.5.1
scipy==1.18.0

# Classical exact optimization (MILP)
PuLP==3.3.2
ortools==9.15.6755

# Quantum-inspired optimization (QUBO/BQM)
# NOTE: fill in the exact versions from your venv — these were not present
# in the pasted pip freeze output but are required by the Task 4/5 notebooks.
dimod
neal
dwave-tabu

# Optional: only needed to open/run the Jupyter notebooks interactively.
# Not required to run the underlying .py scripts, if you have them.
jupyter==1.1.1
jupyterlab==4.6.1
ipykernel==7.3.0